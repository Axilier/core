/** @format */

// @flow
import * as React from 'react';
import '../../css/TileList.css';
import { TabListProps } from '../../Types';

const TileList = ({ children }: TabListProps) => {
    return <div>{children}</div>;
};

export default TileList;
