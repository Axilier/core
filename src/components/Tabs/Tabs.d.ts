import { TabsProps } from '../../Types';
declare const Tabs: ({ showNotSelectedShadow, tabNotSelectedColor, tabSelectedColor, tabIndicatorColor, tabFontColor, onChange, children, orientation, width, }: TabsProps) => JSX.Element;
export default Tabs;
