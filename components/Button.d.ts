/// <reference types="react" />
import { ButtonProps } from '../Types';
declare const Button: ({ btnType, variant, btnColor, btnStyles, btnIconBackgroundColor, disabled, btnIcon, children, onClick, style, className, id, }: ButtonProps) => JSX.Element;
export default Button;
