/// <reference types="react" />
import { TabProps } from '../../Types';
declare const Tab: ({ orientation, showNotSelectedShadow, children, onClick, }: TabProps) => JSX.Element;
export default Tab;
